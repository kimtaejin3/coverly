# packages/shared

Shared types/contracts between `apps/web` and `apps/worker` (job status enum, metrics schema).
Empty until Phase 2 (Supabase) — the worker's `GenerationMetrics.to_dict()` is the reference shape.
